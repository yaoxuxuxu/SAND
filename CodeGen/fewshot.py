from .modelManager import ModelManager
class baseline(ModelManager):
    def __init__(self):
        super().__init__()
        self.user_add("Use the script Programming language stone to complete it")
    